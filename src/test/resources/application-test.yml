spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:room_booking_db}_test
    username: ${DB_USERNAME:room_user}
    password: ${DB_PASSWORD:room123456}
  flyway:
    enabled: true
    baseline-on-migrate: true

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000

logging:
  level:
    root: WARN
    com.roombooking: INFO
    org.flywaydb: WARN
